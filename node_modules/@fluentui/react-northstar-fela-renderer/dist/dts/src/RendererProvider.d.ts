import * as React from 'react';
import { FelaRenderer } from './types';
declare type RendererProviderProps = {
    renderer: FelaRenderer;
    rehydrate?: boolean;
    targetDocument?: Document;
};
declare module 'fela-dom' {
    function render(renderer: FelaRenderer, targetDocument?: Document): void;
    function rehydrate(renderer: FelaRenderer, targetDocument?: Document): void;
}
export declare class RendererProvider extends React.Component<RendererProviderProps, Record<string, unknown>> {
    constructor(props: RendererProviderProps);
    componentDidUpdate(prevProps: RendererProviderProps): void;
    _renderStyle(): void;
    render(): JSX.Element;
}
export {};
